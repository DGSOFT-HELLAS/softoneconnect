/// add this to the table to have custom size for each column

  {...{
                        style: {
                          width: table.getCenterTotalSize()
                        }
                      }}